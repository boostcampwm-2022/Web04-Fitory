import * as s from "./style";

export interface ProfileImageContainerProps{
  isProfile: boolean;
}
const ProfileImageContainer = ({isProfile}: ProfileImageContainerProps) => {
  return(
    <s.Wrapper>
      <h1>hi</h1>
    </s.Wrapper>
  );
}

export default ProfileImageContainer